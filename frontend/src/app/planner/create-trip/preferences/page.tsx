import { PreferencesPage } from "@/features/createTrip/PreferencesPage"

export const metadata = {
  title: "Trip Preferences & Personalization — Trip Mind AI",
  description: "Craft your custom travel vibe, select interests, lodging levels, and pace suggestions curated by AI."
}

export default function PreferencesRoute() {
  return <PreferencesPage />
}
